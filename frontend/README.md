# AI Voice Draw — Frontend

Vue 3 + TypeScript 前端，提供语音录音、提示词输入与图像预览。

## 启动

```bash
npm install
npm run dev
```

访问：http://localhost:5173

后端项目位于同级目录 `../backend/`，开发时通过 Vite 代理转发 API 请求到 `http://127.0.0.1:8000`。
